
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	Если ПараметрыВыполненияКоманды.Источник=Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ПараметрыВыполненияКоманды.Источник.ИмяФормы = "Справочник.Номенклатура.Форма.ФормаЭлемента" Тогда
		НоменклатураВДокументахКлиент.ПодготовитьРеквизитыСтрокиПоЭлементу(ПараметрыВыполненияКоманды, "ЗаказПоставщику");
	Иначе
		НоменклатураВДокументахКлиент.ПодготовитьРеквизитыСтрокСписка(ПараметрыВыполненияКоманды, "ЗаказПоставщику");
	КонецЕсли;
	
КонецПроцедуры
