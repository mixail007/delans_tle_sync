Процедура ПодключитьПечатнуюФорму(ПараметрКоманды, ПараметрыВыполненияКоманды) Экспорт
	ОбъектыНазначения = Новый Массив();
	ОбъектыНазначения.Добавить(ПараметрКоманды[0]);
	ПараметрыОткрытия = Новый Структура("ОбъектыНазначения", ОбъектыНазначения);
	Форма = ПолучитьФорму("ОбщаяФорма.CTSM_EfsolПервичныйДокумент", ПараметрыОткрытия);
	Форма.Открыть();
КонецПроцедуры